{
  "name": "replay-service",
  "private": true,
  "workspaces": [
    "projects/*"
  ],
  "scripts": {
    "build": "npx lerna run build --parallel",
    "start": "npx lerna run start --parallel",
    "start:dev:api": "npx lerna run start:dev --scope=@replay-service/api",
    "test": "npx lerna run test --parallel",
    "test:e2e": "npx lerna run test:e2e --parallel",
    "lint": "eslint . --max-warnings=0",
    "typescript": "tsc --noEmit",
    "format": "prettier --write \"projects/**/*.ts\""
  },
  "dependencies": {
    "lerna": "^9.0.7"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.2.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.0.0"
  },
  "packageManager": "yarn@1.22.22"
}
